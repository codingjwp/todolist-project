
import { test, expect} from 'vitest';
import { screen } from '@testing-library/react';
import { router, customRender } from './Home.test'


test('navigate to sign up or sign in page when Link click', () => {
  customRender();
});

test('render sign up of sign in button', () => {
  customRender();
});